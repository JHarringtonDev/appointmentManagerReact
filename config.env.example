# Copy this to `config.env` at the project root (do NOT commit secrets).
# Example values — replace with your actual MongoDB connection string and secrets.

# MongoDB connection string (Atlas example)
ATLAS_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/mydatabase?retryWrites=true&w=majority

# Alternative common name used by some providers
#MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/mydatabase?retryWrites=true&w=majority

# Server port
PORT=5000

# NODE_ENV=production
# Any other secrets (Auth0 client id/secret, etc.) should be added here as needed
